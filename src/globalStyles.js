import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    h1: {
        fontSize: 40,
        textAlign: 'center',
        margin: 10,
    }, h2: {
        fontSize: 20,
        textAlign: 'center',
        margin: 10,
    },
});

export default styles;